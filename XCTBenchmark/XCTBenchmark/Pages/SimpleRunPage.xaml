<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XCTBenchmark.Pages.SimpleRunPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <ContentPage.Content>
        <ScrollView x:Name="Scroller">
            <Grid RowSpacing="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Button
                    x:Name="Run"
                    Padding="10,10,10,0"
                    Clicked="Button_Clicked"
                    Text="Run" />
                <ScrollView Grid.Row="1" Orientation="Both">
                    <Label
                        x:Name="Summary"
                        Grid.Row="1"
                        Padding="10"
                        FontFamily="monospace" />
                </ScrollView>
                <ActivityIndicator
                    x:Name="Indicator"
                    Grid.RowSpan="2"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"
                    Color="DarkGray" />
            </Grid>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>